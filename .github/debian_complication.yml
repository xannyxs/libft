name: Libft macOS compile check CI

on:
	push:
		branches: [ master ]
	pull_request:
		branches: [ master ]

jobs:
	stevejobs:

		runs-on: debian-latest

		steps:
		- uses: actions/checkout@v2
		- name: make re
			run:  |
						make re
